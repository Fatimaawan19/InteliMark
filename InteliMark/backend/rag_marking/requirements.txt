# PyTorch CPU-only setup for Windows (avoids CUDA/DLL issues)
# 
# CRITICAL: Install PyTorch FIRST from the CPU index:
#   pip install --index-url https://download.pytorch.org/whl/cpu torch==2.2.0 torchvision==0.17.0 torchaudio==2.2.0
#
# Then install remaining packages:
#   pip install -r requirements.txt
#
# This two-step process ensures PyTorch CPU-only is installed before dependencies try to use it.

# No torch/torchvision/torchaudio here - install them manually first with the index URL above

PyMuPDF==1.25.3
pdfplumber==0.11.5
faiss-cpu>=1.7.0  # Local vector database (no API keys needed)

# Embedding: Use compatible versions of sentence-transformers and transformers
sentence-transformers==3.1.0  # Downgraded for stability (3.4.1 has dependency conflicts)
transformers==4.38.0  # Compatible with sentence-transformers 3.1.0

pymongo>=3.12.0
python-pptx>=0.6.23
python-docx>=1.1.2
pytesseract>=0.3.10
Pillow>=10.0.0
numpy<2.0
requests>=2.28.0  # For API fallback embeddings


